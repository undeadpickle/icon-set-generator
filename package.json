{
  "name": "icon-set-generator",
  "version": "1.0.0",
  "description": "Figma plugin to generate icon component sets with multiple size variants",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.97.0",
    "typescript": "^5.3.3"
  }
}
